public class Testpakati {

    int n =1;
    double x = 0;
    double s;
    while (ture) {
        s = 1.0 / (n * n);
        if (s <= 0.01) {
            break;
        }
        x = x + 5;
        n++;
    }
}